#SOURCE   DEST    POLICY     LOGLEVEL   LIMIT
#
# Standalone host: The node has access to the public Internet. Access to LAN (RFC 1918) is blocked
# except for whitelisted addresses. This is implemented in the `rules` file.
fw        net     ACCEPT
net       fw      DROP       info
all       all     DROP       info
