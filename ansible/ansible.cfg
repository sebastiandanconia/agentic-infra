[defaults]
inventory = hosts.ini
interpreter_python = auto_silent
# Roles live in ansible/roles/. Resolves correctly whether roles_path is
# taken as CWD-relative or config-relative, as long as you run from ansible/.
roles_path = roles

[ssh_connection]
control_path = %(directory)s/%%h-%%p-%%r
pipelining = True
